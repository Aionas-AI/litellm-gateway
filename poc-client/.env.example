# Gateway base URL (OpenAI-compatible endpoint). Note the /v1 suffix.
LITELLM_BASE_URL=https://56.228.19.15.sslip.io/v1

# A virtual key minted on the gateway (scoped to claude-opus). NEVER commit the real key.
LITELLM_API_KEY=sk-your-virtual-key

# Model alias as defined in the gateway's config.yaml
LITELLM_MODEL=claude-opus
